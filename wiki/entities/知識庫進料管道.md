---
tags: [entity]
updated: 2026-08-30
---

# 知識庫進料管道

這個知識庫有四條資料進來的路徑。兩條是既有的，兩條是 2026-08-28 新增的本機工具。

## 重點

| 管道 | 進到哪 | 怎麼運作 | 狀態 |
|---|---|---|---|
| **Telegram → n8n 自動存檔** | `raw/` | 傳連結給 Telegram bot，n8n 抓網頁、Gemini 整理、推 GitHub | ⚠️ **運作中但有兩個缺陷**，見下 |
| Obsidian Web Clipper | `Clippings/` | 瀏覽器擴充功能剪網頁 | 正常 |
| YouTube 逐字稿 | `raw/youtube_<日期>_<影片ID>.md` | `tools/capture.py --youtube <網址>` | 2026-08-28 新增 |
| 截圖（含 X、限動、付費內容、**有圖的 IG／Threads 貼文**） | `raw/shot_<日期>_<序號>.md` + `raw/assets/` | `tools/capture.py --shot <圖…>`，可一次多張 | 2026-08-30 起支援多圖 |
| **電腦 Chrome 補救**（推薦） | 同上 | 跟 Claude 說「補這篇」＋貼網址，它用你電腦上已登入的 Chrome 開貼文、自動截圖、存檔、讀圖寫內容 | 2026-08-30 實測成功 |

---

## Telegram → n8n 管道的完整結構

**位置**：n8n 自架於 Zeabur，`https://maxyyy.zeabur.app`
**Workflow ID**：`gMt3TVBIagJGA6PG`
**查明日期**：2026-08-30

```
你在 Telegram 傳一個連結給 bot
        ↓
  Telegram Trigger        接收訊息
        ↓
  Extract URL             取出網址
        ↓
  Jina Reader             把網頁轉成純文字        ← 缺陷 A
        ↓
  Prepare Gemini Prompt   組提示詞
        ↓
  Call Gemini API         gemini-2.5-flash 整理   ← 缺陷 B
        ↓
  Build Markdown          組成 .md
        ↓
  Prepare GitHub Payload → Commit to GitHub
        ↓
  Check Result → Send Telegram Reply（回報結果給你）
        ↓
  Obsidian github-sync 外掛同步下來 → 出現在 raw/
```

> **Jina Reader** 是免費服務（`r.jina.ai`），給它網址、回傳網頁純文字。
> **關鍵限制：它只回傳文字，不回傳圖片。**

---

## ⚠️ 缺陷 A：Jina Reader 讀不動 Threads / IG

Threads 與 Instagram 的內容靠 JavaScript 動態產生，而且擋未登入訪客。Jina 抓到的常常是**錯誤頁**而不是貼文本身。

**造成的後果**

1. **6 篇「404 空頁」**（`threads_2026-04-29` 系列）——不是貼文被刪，是 Jina 讀不到，Gemini 收到錯誤頁的文字就照實摘要了一遍。這 6 篇已於 2026-08-30 刪除，**原始網址保留在 [[待補資料清單]]**，管道修好後可以重傳。
2. **四個月來一張圖都沒進來**。`raw/assets/` 到 2026-08-30 前都是空的。有圖的貼文（例如十大封裝技術那篇，內容整個在圖裡）等於完全漏掉。

**這條無解**——平台限制，Jina 拿不到需要登入才看得到的圖。**有圖的貼文請直接走截圖管道。**

## ⚠️ 缺陷 B：Gemini 輸出長度上限造成截斷

**15 篇裡有 10 篇斷在句子中間**，例如：

```
raw/threads_2026-05-27（桃機停車）
  …「桃機P4停車場：位於二航     ← 就這樣沒了
```

正確內容還有「限 100 名預約、要提早 20 天登記、最少停 3 天」——**最有用的部分全部掉了**。

### 已確認的原因（2026-08-30 查看節點設定）

`Call Gemini API` 節點的 Body / JSON 原本是：

```javascript
{{ JSON.stringify({ contents: [{ parts: [{ text: $json.prompt }] }],
   generationConfig: { temperature: 0.3, maxOutputTokens: 1500 } }) }}
```

**兩個問題**

1. **`maxOutputTokens: 1500` 太小**。1500 token 中文大約 1000–1500 字，換成 UTF-8 約 2000–4500 bytes——被截斷的檔案正好都是 2000–3400 bytes，**數字完全吻合**。
2. **沒有 `thinkingConfig`**。`gemini-2.5-flash` 預設會先「思考」再回答，**思考也吃這 1500 的額度**，實際能寫的可能只剩五、六百 token。

### 修法一：把 JSON 換成這行（最快，改完存檔即生效）

```javascript
{{ JSON.stringify({ contents: [{ parts: [{ text: $json.prompt }] }], generationConfig: { temperature: 0.3, maxOutputTokens: 8192, thinkingConfig: { thinkingBudget: 0 } } }) }}
```

| 改動 | 為什麼 |
|---|---|
| `maxOutputTokens: 1500 → 8192` | 額度給足，長貼文寫得完 |
| 加 `thinkingConfig: { thinkingBudget: 0 }` | 關掉思考，額度全部用在寫筆記 |

⚠️ `thinkingConfig` 要放在 `generationConfig` **裡面**。gemini-2.5-flash 允許設 0；若改用 2.5 Pro，Pro 不能關思考，要改成 `1024` 之類的正整數。

### 修法二：把 Jina 原文一併存進檔案（更根本）

在 `Build Markdown` 節點，把 Jina Reader 抓到的原文也寫進去（例如放在筆記最後的「## 原文」段落）。這樣就算摘要被截斷，原始內容還在。

→ **消化的工作是在 Obsidian 裡由 Claude 做，管道只要負責完整搬運就好**，不需要它先幫忙濃縮。

### 修法三：截斷時主動告警（加保險）

在 `Check Result` 節點檢查 Gemini 回應的 `candidates[0].finishReason`，若為 `MAX_TOKENS` 就在 Telegram 回一句「⚠️ 這篇被截斷了」。當下就知道，不用等四個月後才發現。

---

## 查證過程紀錄（2026-08-30）

一開始完全不知道這條管道在哪，排除的順序：

| 查了什麼 | 結果 |
|---|---|
| repo 有沒有 GitHub Actions | 沒有 |
| commit 的作者 | `darcon25 <chientsungliu@gmail.com>`，不是 GitHub 網頁的 `noreply@github.com` → 外部工具用個人身分推的 |
| repo 部署金鑰 / 安裝的 GitHub App | 都沒有 → 用個人存取權杖（PAT），API 查不到 |
| 本機檔案裡的線索 | `n8n DCA 定投/README.md` 記著 n8n 位置 `https://maxyyy.zeabur.app` |
| n8n workflow | 直接看到完整流程圖 |

**教訓**：自己設的自動化要留一筆紀錄，否則四個月後就變成黑盒子。這頁就是那筆紀錄。

## 怎麼知道哪些檔案有問題

這是整套機制能不能運作的關鍵——**如果偵測不到，修得再好也沒用**。

### 截斷偵測（已實作，但有盲點）

`tools/health_check.py` 的規則：Threads/IG 來源的檔案，正文最後一行若**沒有句尾標點**（。！？）」等），判定為截斷。

- ✅ 抓到 10 篇
- ⚠️ **盲點**：若 Gemini 剛好在句號處用完額度，就抓不到

→ **唯一 100% 可靠的方法在 n8n**：檢查 Gemini 回應的 `candidates[0].finishReason`，等於 `MAX_TOKENS` 就是被截斷，沒有模糊空間。這就是「修法三」存在的理由。

### 漏圖偵測（2026-08-31 加入，刻意用寬鬆規則）

**先講一個失敗的嘗試**：原本想用關鍵字掃描（找「一張圖」「如下圖」這類線索），結果**它漏掉了十大封裝技術那篇**——因為那句「我整理了一張圖」在貼文開頭，而開頭連同整段被截斷掉了。**文字被切、線索也跟著被切，關鍵字法在最需要它的時候失效。**

**改用的規則**：Threads 與 IG 本來就是以圖為主的平台，而 Jina Reader 拿不到圖。所以反過來——**凡是這兩個平台來的貼文，只要沒有對應的截圖檔，一律列為「圖片未確認」**。

- 噪音大（目前 13 筆全部列出），但**不會漏**
- 你逐篇看過後，把檔名記進 `data/known_issues.json` 的 `media_checked`，健檢就不再提醒
- 文中若有「一張圖」這類線索會排在前面，當優先度參考

### 更精準的作法（需要改 n8n）

Jina Reader 回傳的是 **markdown**，裡面的圖片會變成 `![](網址)` 語法。若在 `Build Markdown` 節點數一下有幾個，寫進 frontmatter：

```yaml
media_count: 6
```

健檢就能精準比對「原文有 6 張圖，但 assets 一張都沒有」，不必再用寬鬆規則猜。**這是修法二（存 Jina 原文）的附加好處。**

## 補救路徑：用電腦 Chrome 抓圖（2026-08-30 實測成功）

Jina Reader 拿不到圖，但**你電腦上的 Chrome 已經登入 Threads/IG**，可以看到完整貼文。流程：

1. 你跟 Claude 說「補這篇」並貼上網址
2. Claude 在你的 Chrome 開啟貼文 → 點開圖片放大 → 自動截圖
3. 截圖存進 `raw/assets/`，同時建立 `raw/shot_<日期>_<序號>.md`
4. Claude 直接讀圖，把內容寫進筆記

**你完全不用碰手機，也不會在相簿堆一堆截圖。**

**首次實測**：`threads_2026-07-14`（十大封裝技術）四個月前只存到殘缺片段，2026-08-30 用這條路徑完整補回整張技術總表，寫入 `raw/shot_2026-08-30_01.md` 與 [[HBM4與先進封裝]]。

### 這條路的限制（重要）

| 限制 | 說明 |
|---|---|
| **電腦要開著、要有網路** | 休眠或斷線時完全不會執行。**這不是背景自動化** |
| **要有對話** | 需要 Claude 在對話中驅動瀏覽器，不能排程 |
| 輪播圖 | 目前只能抓到第 1 張，燈箱沒有換頁按鈕（Threads 版面問題） |

### 所以兩條管道要並存

| | Telegram → n8n（雲端） | 電腦 Chrome（Claude 驅動） |
|---|---|---|
| 何時可用 | **24 小時**，手機在外面就能存 | 只有你在電腦前、且在對話中 |
| 負責 | **不漏接**——看到就先存起來 | **補齊**——把截斷內容與圖片補回來 |
| 圖片 | ❌ 拿不到 | ✅ 拿得到 |

**運作方式**：n8n 隨時捕捉 → 每日健檢自動標記哪些被截斷 → 你開電腦時說一句「補一下」，Claude 照 [[health]] 的清單批次處理。**你不用記得哪些要補。**

## 相關連結

- [[資料源與限制]]、[[待補資料清單]]、[[health]]
- [[個人知識庫與第二大腦]]
